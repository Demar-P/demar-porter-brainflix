import React from 'react';

const Comments = ({ comments }) => {
    return (
        <div>
        {comments.map((comment, ) => (
        <div key={index}>
        <p>Author: {comment.}</p>
        <p>{comment.}</p>
        </div>
))}
        </div>
);
    };

    export default Comments;
